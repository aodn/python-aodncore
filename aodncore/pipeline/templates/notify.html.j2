<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
</head>
<style>
    table {
        border-collapse: collapse;
    }

    th {
        background-color: #C0FFEE;
        font-weight: bold;
    }

    table, td, th {
        border: 1px solid #666;
        padding: 5px;
    }

    .tablewrapper {
        overflow-x: auto;
    }

    .fineprint {
        font-size: 10px;
    }
</style>
<body>
    <p>Dear Uploader,</p>
    <p>Please find below a report on the handling of the following file:</p>
    <div class="tablewrapper">
        {{ html_input_file_table }}
    </div>

    {% if error_details -%}
    <h4>Error details:</h4>
    <pre>{{ error_details }}</pre>
    {%- endif %}

    {% if html_collection_table %}
    <h4>File collection details:</h4>
    <p>The input file resolved to the following child file(s):</p>
    <div class="tablewrapper">
        {{ html_collection_table }}
    </div>
    {% endif %}

    <p>
    Please reply to this message for further information (or contact the relevant AODN project officer).
    Please include the full text of this message in your reply.
    </p>

    <p>Regards,<br>
        Australian Ocean Data Network
    </p>

    <footer>
        <p class="fineprint">
            AODN reference: {{ celery_task_name }}[{{ celery_task_id }}]
        </p>
    </footer>

</body>
</html>
