language: python
python:
  - "2.7"

install:
  - wget http://imos-binary.s3-website-ap-southeast-2.amazonaws.com/jobs/compliance_checker_prod/compliance_checker_prod-latest.whl
  - wget http://imos-binary.s3-website-ap-southeast-2.amazonaws.com/jobs/cc_plugin_imos_prod/cc_plugin_imos_prod-latest.whl
  - pip install compliance_checker_prod-latest.whl
  - pip install cc_plugin_imos_prod-latest.whl
  - pip install -r requirements.txt

script:
  - python setup.py test
