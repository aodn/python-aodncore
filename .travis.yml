language: python
python:
  - "3.5"
  - "3.6"

addons:
  apt:
    packages:
    - libudunits2-dev

install:
  - pip install -r requirements.txt
  - pip install coverage
  - pip install codecov

script:
  - coverage run setup.py test

after_success:
  - codecov
